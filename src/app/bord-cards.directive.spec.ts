import { BordCardsDirective } from './bord-cards.directive';

describe('BordCardsDirective', () => {
  it('should create an instance', () => {
    const directive = new BordCardsDirective();
    expect(directive).toBeTruthy();
  });
});
